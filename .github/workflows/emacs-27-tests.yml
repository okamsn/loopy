name: Test Emacs 27 from Ubuntu 22.04 Repo
on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
jobs:
  emacs-27-tests:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Install Emacs 27 from Ubuntu repo.
        run: sudo apt-get install emacs
      - name: Check Emacs 27 version
        run: emacs --version
      - name: Create the folder
        run: mkdir dependecy-links
      - name: Download Map
        run: wget --output-document="dependecy-links/map.el" "https://git.savannah.gnu.org/cgit/emacs.git/plain/lisp/emacs-lisp/map.el"
      - name: Basic tests
        run: emacs -batch -l ert -l tests/tests.el -f ert-run-tests-batch-and-exit
      - name: Seq tests
        run: emacs -batch -l ert -l tests/seq-tests.el -f ert-run-tests-batch-and-exit
      - name: Pcase tests
        run: emacs -batch -l ert -l tests/pcase-tests.el -f ert-run-tests-batch-and-exit
      - name: Iter tests
        run: emacs -batch -l ert -l tests/iter-tests.el -f ert-run-tests-batch-and-exit
      - run: echo "Now doing Dash tests."
      - name: Download Dash
        run: wget --output-document="dependecy-links/dash.el" "https://raw.githubusercontent.com/magnars/dash.el/master/dash.el"
      - name: Dash tests
        run: emacs -batch -l ert -l tests/dash-tests.el -f ert-run-tests-batch-and-exit
      - run: echo "Now doing miscellaneous tests."
      - name: Miscellaneous
        run: emacs -batch -l ert -l tests/misc-tests.el -f ert-run-tests-batch-and-exit
      - run: echo "🍏 This job's status is ${{ job.status }}."
